#!/usr/bin/env python

# Goals:
# > To generate hexadecimal or binary files based on s16 assembley
# > Use "argv"s to anable multiple assembly files to be stitched into one hex/bin file
# > Use "argv"s to select

# Long Term Goals:
# > Assemble in the command-line/shell then output to hex/bin file
