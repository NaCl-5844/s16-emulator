xor r1, r1 ;this is a comment - set r1 to 0
lim r0, -3 ;setup for loop
lim r2, 1 ; so is this.
jir 3 ;skip over conditionals
int 0 ;if r3 = -2: halts with code 0
ret ;if r3 = -1: exits code back to main program
add r1, r2, r1
add r2, r2, r1
tfc r3, r2 ;if both cout: r3 = -2, if one cout: r = -1
jrr r0, r3 ;if both cout:interupt, if one cout:return

